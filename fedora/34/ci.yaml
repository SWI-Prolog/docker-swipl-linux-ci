# Fedora 7 defaults

default:
  generator:
    cmake: Ninja
    command: ninja
  ctest_env:
      LANG: en_US.utf8

configs:
  - full:
      comment: Default complete build
  - pgo:
      comment: PGO optimized buil
      cmake_flags: -DCMAKE_BUILD_TYPE=PGO
